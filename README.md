# MODIFICATION!

If you make a modification to the `AssemblyModel` `struct` or ***any type it contains*** you must make sure to update `types.h` in this repo and any `#insert` elements that define it.
